# Custom ChromaDB image with curl for health checks
FROM chromadb/chroma:latest

# Install curl for health checks
USER root
RUN apt-get update && \
    apt-get install -y curl && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Don't switch user - let the base image handle it
# The original CMD will be inherited from the base image
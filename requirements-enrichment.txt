# WhisperEngine Enrichment Worker Requirements
# Minimal dependencies for background enrichment processing

# Core dependencies
qdrant-client>=1.7.0
asyncpg>=0.29.0
psutil>=5.9.0
influxdb-client>=1.38.0  # Required for emoji feedback InfluxDB storage

# LLM client dependencies (OpenRouter)
openai>=1.0.0
httpx>=0.24.0
requests>=2.31.0

# Logging and utilities
python-dotenv>=1.0.0

# Optional NLP preprocessing (spaCy for entity extraction, dependency parsing)
# Enables hybrid LLM+local preprocessing in enrichment worker
# Reduces LLM token usage and improves extraction accuracy
spacy==3.8.7
